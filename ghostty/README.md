### Setup Ghosty

- Create a .config/ghostty folder in your home directory
- Copy config and Ghostty.icns into the newly created ghostty folder.
- Save config
- Reload Ghostty from the menu bar Ghostty > Reload Configuration 